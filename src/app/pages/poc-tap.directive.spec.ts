import { PocTapDirective } from './poc-tap.directive';

describe('PocTapDirective', () => {
  it('should create an instance', () => {
    const directive = new PocTapDirective();
    expect(directive).toBeTruthy();
  });
});
